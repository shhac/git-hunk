name: Build Artifacts

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to build from'
        required: false
        default: ''

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: Build for ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - platform: x86_64-macos
            os: macos-latest
            target: x86_64-macos
          - platform: aarch64-macos
            os: macos-latest
            target: aarch64-macos
          - platform: x86_64-linux
            os: ubuntu-latest
            target: x86_64-linux
          - platform: aarch64-linux
            os: ubuntu-latest
            target: aarch64-linux

    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ github.event.inputs.branch || github.ref }}

      - name: Setup Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2.2.1
        with:
          version: '0.16.0'

      - name: Build for ${{ matrix.platform }}
        run: zig build -Doptimize=ReleaseFast -Dtarget=${{ matrix.target }}

      - name: Create tarball
        run: |
          cd zig-out/bin
          tar -czf ../../git-hunk-${{ matrix.platform }}.tar.gz git-hunk
          cd ../..

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: git-hunk-${{ matrix.platform }}
          path: git-hunk-${{ matrix.platform }}.tar.gz
          retention-days: 30

  create-universal-macos:
    name: Create Universal macOS Binary
    runs-on: macos-latest
    needs: build
    steps:
      - name: Download x86_64 artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: git-hunk-x86_64-macos

      - name: Download aarch64 artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: git-hunk-aarch64-macos

      - name: Extract binaries
        run: |
          tar -xzf git-hunk-x86_64-macos.tar.gz
          mv git-hunk git-hunk-x86_64
          tar -xzf git-hunk-aarch64-macos.tar.gz
          mv git-hunk git-hunk-aarch64

      - name: Create universal binary
        run: |
          lipo -create -output git-hunk git-hunk-x86_64 git-hunk-aarch64
          tar -czf git-hunk-macos-universal.tar.gz git-hunk

      - name: Upload universal artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: git-hunk-macos-universal
          path: git-hunk-macos-universal.tar.gz
          retention-days: 30

  summary:
    name: Build Summary
    runs-on: ubuntu-latest
    needs: [build, create-universal-macos]
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1

      - name: List artifacts
        run: |
          echo "## Build Artifacts Created" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Available downloads:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          for dir in git-hunk-*/; do
            filename=$(ls "$dir"*.tar.gz 2>/dev/null | head -1)
            if [ -n "$filename" ]; then
              size=$(du -h "$filename" | cut -f1)
              echo "- ${filename#*/} ($size)" >> $GITHUB_STEP_SUMMARY
            fi
          done
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Artifacts will be available for 30 days." >> $GITHUB_STEP_SUMMARY
